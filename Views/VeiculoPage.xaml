<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Frotas.Views.VeiculoPage"
             Title="Veiculos">
    <StackLayout Padding="10">
        <Button Text="Adicionar Veículo"  />

        <CollectionView x:Name="ListaVeiculos" SelectionMode="Single" >
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Margin="5" BorderColor="Gray">
                        <Label Text="{Binding Nome}" FontSize="18" />
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>